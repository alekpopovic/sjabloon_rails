<!doctype html>
<html>
<head>
  <%= render "shared/head" %>
</head>

<body class="bg-gray-200 <%= body_class(yield(:body_class)) %>">
  <%= render "shared/flash" %>

  <div class="">
    <%= link_to Rails.configuration.application_name, root_url, class: "block my-8 text-2xl text-center font-bold text-black hover:text-grey-dark no-underline" %>

    <%= yield %>
  </div>

  <%= tag.div nil, data: { controller: 'sjabloon--fill-form' } if Rails.env.development? %>
</body>
</html>


